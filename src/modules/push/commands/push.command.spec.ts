import { describe, it } from 'vitest';

describe.skip('PushCommand (removed)', () => {
  it('is no longer available as a CLI command; functionality handled via build flow', () => {
    // intentionally skipped
  });
});
